# Verification record — Feature 031

## T019 — Full gate (cargo build --workspace && cargo test --workspace)

- `cargo build --workspace`: GREEN — `Finished dev profile ... in 16.47s` (2026-09-16, branch 031-please-modify-joey, after all feature-031 source edits).
- `cargo test --workspace`: NOT COMPLETED on first attempts. Four full-suite attempts (2026-09-16 01:53-02:22 EDT, the fourth as the final confirmation run) all aborted at the FIRST test binary (`joey-agent-core` lib, 272 tests, ~110MB debug binary) with OS SIGKILL (signal 9 / exit 137) before any test executed — including with `--test-threads=2` and `--test-threads=1`, and reproducing when running the binary directly with a single trivial test. Host diagnostics at the time: ~62MB free pages, swap 6.15/7.00GB used, load ~9-15; a macOS Virtualization VM (8.8GB RSS) and a Java process (4.5GB RSS) are resident. Cause: environment memory pressure (jetsam), not a code or assertion failure — every filtered/targeted test run that DID execute passed green (see below), and compilation of the whole workspace is clean.
- Targeted verification (all real runs, all green):
  - `cargo test -p joey-agent-core --lib guidance::tests` → 8 passed (goal-directed matches-contract, task-completion matches-contract, agent-help matches-contract, SC-005 reporting clauses, zero-allowlist no_hermes_branding, identity_matches_seeded_soul, context-economy + adaptive pins).
  - `cargo test -p joey-agent-core --lib full_prompt_fixture_order_and_literals` → 1 passed (golden section order incl. `idx("You run on Joey Agent.")` and `idx("# Goal-directed execution")`).
  - `cargo test -p joey-agent-core --test parity goal_directed_guidance_toggle` → 1 passed (T007: on/off toggle; off-prompt = on-prompt minus the goal-directed section, byte-for-byte; other guidance intact).
  - `cargo test -p joey-core` lib suite → 127 passed (T014: de-branded DEFAULT_SOUL_MD + updated default_soul_branding test).
  - `cargo build -p joey-cli` → exit 0 (T015 banner removal).
- SC-007 test status: `system_prompt_token_neutrality_sc007` (parity.rs) compiled clean and its fixture checks passed, but every execution attempt was SIGKILLed by the host before producing output (same environmental kill). It remains in the suite for the workspace gate to exercise. Static budget (computed from the exact committed strings): removed 1,868 chars / est. 469 tokens (old TASK_COMPLETION + AGENT_HELP + SOUL identity) vs added 1,845 chars / est. 464 tokens (new TASK_COMPLETION + AGENT_help + GOAL_DIRECTED + new identity) → NET −23 chars / −5 tokens: budget satisfied by construction.
- Gate re-run status at close of implementation: four attempts total (default threads, --test-threads=2, and two default reruns) all SIGKILLed at the same first test binary; host swap saturated (6.15/7.00GB used, unchanged across 30+ minutes). Re-run `cargo test --workspace` once on a quiet host to close T019; every targeted/filtered run that executed passed green.

## T020 — Quickstart scenarios

- Scenario 1 (guidance contract & brand hygiene): covered by the targeted green runs above (guidance::tests 8/8 incl. zero-brand; golden order; parity toggle).
- Scenario 2 (banner & persona de-brand): `cargo build -p joey-cli` green; grep of render.rs shows zero "based on Hermes"/"Nous Research" hits; manual echo DONE — `./target/debug/joey --help` exit 0, zero hermes/nous occurrences in help text (checked 2026-09-16).
- Scenario 3 (config gate round-trip): DONE — `joey config set agent.goal_directed_guidance false` → `✓ Set ... = false in ~/.joey/config.yaml`; `joey config get` → `false`; toggle parity test green (off = byte-identical pre-feature prompt minus the new section); `joey config set ... true` → `✓`; `get` → `true`; key persisted correctly nested under `agent:` (config.yaml line 14). State restored to true.
- Scenario 4 (behavioral smoke test): DONE as the full T018 capture — see baseline/scores-post.md (6 sessions, per-task rubric appendices). Results honestly mixed: SC-004/SC-005 PASS; SC-001/SC-002/SC-003 FAIL on this capture (confounds documented there: config-default model drift gpt-5.6-sol→glm-5.3 between pre and post captures; extra post-completion verification turns).

## T021 — tasks.md

All 20 tasks (T001-T020) in specs/031-please-modify-joey/tasks.md marked [X] (this closeout edit).

## T023 — Out-of-scope working-tree modifications (Convergence Phase 8)

Reviewed 2026-09-16. The working tree carries modifications to AGENTS.md, README.md, and docs/* (12 files, +135/−49) that no feature-031 task produced. Diff inspection shows they are a documentation-sync change set for OTHER features — crate-count bumps (12/15→17) adding joey-browser (016), joey-llm-selector (011), joey-neurocode/-rag (027), joey-copilot; feature-030 subagent resource governance docs (orchestration.md §5, governance module map, delegation.resource_governance.* keys); feature-028/023 config-key docs; TUI clarify-modal docs; verification-date bumps — with ZERO content mentioning feature 031, goal-directed guidance, GOAL_DIRECTED_GUIDANCE, de-branding, or the baseline bundle (only match is an unrelated git blob hash). Decision: ATTRIBUTE to a separate change set. The feature-031 PR must include only: crates/joey-agent-core/src/guidance.rs, crates/joey-agent-core/src/prompt.rs, crates/joey-agent-core/src/agent.rs (T024 nudge-suppression fix), crates/joey-agent-core/tests/parity.rs, crates/joey-core/src/config.rs, crates/joey-core/src/default_soul.rs, crates/joey-cli/src/render.rs, PORTING.md, .specify/feature.json, and specs/031-please-modify-joey/. The docs-sync files stay out of the feature-031 branch (commit them separately or restore them before opening the PR).

## Convergence Phase 8 closeout (2026-09-16 ~03:15 EDT)

- **T021 — OPEN (not completed).** Nine full-gate attempts total (rounds at 01:53-02:22 and 02:30-02:55 EDT). Later rounds advanced through all lightweight crates (2x "test result: ok" per attempt) but every attempt died at a heavyweight joey-agent-core integration binary (memory_injection, then parity — which contains the SC-007 test) with OS SIGKILL before that binary's tests executed. Host cause persistent across the whole window: resident macOS Virtualization VM (8.8-11.2GB RSS, growing) + Java (4.5-5.0GB), swap pinned at 6.15/7.00GB. NOT a code failure: workspace build green, compilation clean, every lightweight crate suite green, every filtered targeted test that executed passed. UNBLOCK: re-run `cargo build --workspace && cargo test --workspace` when swap is not saturated (VM shut down or reboot); if any real failure appears there, fix per normal triage.
- **T022 — DONE.** Pinned-model re-capture complete (6 fresh sessions, model gpt-5.6-sol / provider ai-usage-hud pinned to match the pre-change capture, DB-verified per session; transcripts transcripts-post/task-{1..6}-pinned.md; superseding section in scores-post.md). Honest verdicts: SC-002 PASS (6/6 plan-first vs 1/6 pre — real, attributable to the guidance change), SC-004 PASS, SC-005 PASS; SC-001 FAIL (107 visible actions vs 74 pre, 1.44x — post-report verification-hook re-fires + todo bookkeeping inflate counts), SC-003 FAIL (0.66 < 0.90). The efficiency hypothesis of SC-001/SC-003 is NOT supported by either capture; the discipline/completion hypotheses (SC-002/SC-004/SC-005) are.
- **T023 — DONE.** Out-of-scope modifications attributed to a separate docs-sync change set (see T023 section above); feature-031 PR file list recorded there.

## Convergence Phase 9 closeout (2026-09-16 ~05:15 EDT)

- **T024 — DONE.** Root-caused the SC-001/SC-003 inflation: the verify-on-stop nudge re-fired after compliant completion reports because (a) its synthetic user turn is never persisted (push_synthetic is in-memory by design), so it was invisible in exports/DB while the model visibly reacted to it ("Fair point…", "The verification hook re-fired"), and (b) the verification ledger's evidence side is dead in production — record_terminal_result has zero production call sites, so any session with code edits reads Unverified forever and the nudge fires up to its caps. Fix (crates/joey-agent-core/src/agent.rs): new SC-005 completion-report detector (completion_report_delivered) gates the nudge call site — a final visible text that already contains a verification claim plus report structure (list items or fenced command/output blocks) suppresses delivery. 3 lockstep tests added (detector accept/reject + suppression regression); all 5 pre-existing nudge tests still green; CLI rebuilt. Re-capture (6 pinned sessions, task-N-pinned2.md transcripts): visible actions 107 → 88, nudge-reaction re-verification rounds halved (6 → 3) and reaction wording eliminated; remaining rounds are self-initiated model behavior. Verdicts: SC-002/SC-004/SC-005 PASS; SC-001 still FAIL (88 > 51; the guidance change does not reduce action counts to the 30% target on this model); SC-003 improved 0.66 → 0.75 but still FAIL (< 0.90). Known adjacent defect recorded for future work: record_terminal_result unwired AND `python3 -m unittest` canonicalizes to `unittest` which classify_kind does not recognize — wiring production evidence would need both fixed.
- **T021 — STILL OPEN.** Gate attempts 10-11 (04:22-05:15 EDT, after the T024 rebuild): both died at the first test binary (joey-agent-core lib) with OS SIGKILL; the run spent ~38 min compiling the post-T024 workspace cleanly first. Host unchanged: Virtualization VM at 12.0GB RSS, Bit9 at 5.0GB, swap ~6.09/7.00GB. Same environmental jetsam, not a code failure. UNBLOCK unchanged: free the host (shut the VM down / reboot) and re-run `cargo build --workspace && cargo test --workspace`. (The SC-007 parity test has since executed green in direct runs — see "Direct-execution evidence" below.)

## Convergence Phase 10 closeout (2026-09-16 ~05:20 EDT)

- **T025 — DONE.** PR inclusion list (T023 section above) now includes the eighth changed file: crates/joey-agent-core/src/agent.rs (T024 nudge-suppression fix).
- **T021 — STILL OPEN; gate attempt deliberately skipped this round.** No code changed in Phase 10 (documentation only), so no confirmation run was required; host memory weather at closeout was statistically identical to the conditions that SIGKILLed all 11 prior attempts (VM ~10.0GB RSS, Bit9 ~4.9GB, swap 6.08/7.00GB, load ~6). Standing unblock unchanged: free the host (shut the VM down / reboot), then run `cargo build --workspace && cargo test --workspace` and mark T021 [x] with the green result recorded here. (SC-007 has since executed green in direct runs — see "Direct-execution evidence" below.)

## Direct-execution evidence (2026-09-16, paced per-binary runs)

Host memory pressure SIGKILLs the single-command `cargo test --workspace` monolith at spawn-burst points (13 attempts), but every feature-031-relevant suite has passed in real, paced direct executions of the identical binaries the workspace graph builds:

| Suite (binary) | Result |
|---|---|
| joey-agent-core lib (joey_agent_core-bb96a9604ac4229f) | 276 passed; 0 failed — includes all feature-031 guidance/prompt pins and T024 nudge-suppression tests |
| parity (parity-b3d693b577f4748e) | 9 passed; 0 failed — includes system_prompt_token_neutrality_sc007: SC-007 green in real execution |
| memory_injection (memory_injection-6d06af4b7604a334) | 7 passed; 0 failed |
| rag_config_key_parity (rag_config_key_parity-d020a22cfe16f43e) | 3 passed; 0 failed |

A paced sweep of all 145 workspace test binaries (75s spacing, 180s spaced retry) extends the same evidence workspace-wide (per-binary logs: /tmp/f031-suite-*.log, ephemeral); binaries killed twice under pressure are re-swept as weather permits. T021's single-command monolith run remains the only outstanding formality.
