# Baseline capture notes — Feature 031 (pre-change)

- **Capture date/time:** 2026-09-15 20:14–20:28 EDT (sessions 20:14–20:23; notes 20:28)
- **git rev-parse HEAD:** `420bcab9c3b97f0179ee10d2b8226fd56a904ab0`
- **Working tree:** `git status --porcelain crates/` empty — no source edits; CLI built with `cargo build -p joey-cli` (binary `./target/debug/joey`).
- **~/.joey/SOUL.md:** exists — `-rw-------@ 1 jo110366 staff 525 Jul 22 11:41 /Users/jo110366/.joey/SOUL.md`

## Sessions

All six one-shot runs exited 0. Model `gpt-5.6-sol`, provider `ai-usage-hud`, for every session (from the usage JSONs).

| Task | Session id | Prompt category |
|---|---|---|
| 1 | `20260915_201402_aa4114` | coding (sandbox) |
| 2 | `20260915_201457_da8228` | coding (sandbox) |
| 3 | `20260915_201552_d2f1bc` | analysis (read-only repo) |
| 4 | `20260915_201657_2e5977` | analysis (read-only repo) |
| 5 | `20260915_201806_7c8e5d` | writing (sandbox) |
| 6 | `20260915_202100_00f15a` | multi-file refactor (sandbox) |

## Failed runs

None — all six sessions completed and exported.

## Pre-change rubric scores

(Auditor: assistant text messages counted per rubric — "_(text omitted — tool-call message)_" tool-call carriers are not text messages. Tool-call block counts cross-checked against `sessions.tool_call_count` in the DB and `json.loads` of each `messages.tool_calls` payload; all match.)

| Task | Tool invocations | Assistant messages | Visible actions | Plan-first | On-plan ratio | Success | Completion report | Blocker |
|---|---|---|---|---|---|---|---|---|
| 1 | 9 | 3 | 12 | FAIL | 0.56 (5/9) | success | PASS | n/a |
| 2 | 10 | 3 | 13 | FAIL | 0.50 (5/10) | success | PASS | n/a |
| 3 | 6 | 1 | 7 | FAIL | 0.67 (4/6) | success | PASS | n/a |
| 4 | 7 | 1 | 8 | FAIL | 1.00 (7/7) | success | PASS | n/a |
| 5 | 9 | 1 | 10 | FAIL | 1.00 (9/9) | success | PASS | n/a |
| 6 | 21 | 3 | 24 | PASS | 0.81 (17/21) | success | PASS | n/a |

Aggregate: 62 tool invocations, 12 assistant messages, 74 visible actions; plan-first 1/6; aggregate on-plan 49/62 ≈ 0.79; 6/6 success; 6/6 completion report.

## Prompt fixture

- `baseline/pre-change-prompt.txt` — 47,235 bytes, generated by `/tmp/prompt-probe` (deterministic inputs: `Config::defaults()`, temp-dir `ToolContext`, model `parity-model`, provider `parity-provider`, default toolsets, `pass_session_id: false`).
- Verification: `grep -c "# Finishing the job"` → **1**; `grep -c "based on Hermes Agent by Nous Research"` → **2** (≥1 required). Both checks pass.

## Session-export mechanism (per manifest protocol)

Sessions were exported with the Python script `/tmp/export_session.py` (kept for the post-change capture): reads `~/.joey/state.db`, selects the session row plus `messages WHERE active=1 ORDER BY id`, skips system rows, renders assistant text/tool-call blocks and tool results (4,000-char truncation), writes Markdown. Usage per session: `python3 /tmp/export_session.py <session-id> specs/031-please-modify-joey/baseline/transcripts/task-N.md "task N (pre-change)"`.

## Anomalies

- During appendix writing, a concurrent `joey --yolo` session on this machine appended duplicate "Rubric score (pre-change)" sections to the six transcripts (tasks 1 and 2 with a divergent counting method: 7 assistant messages / 16 and 15 visible actions, counting "_(text omitted)_" tool-call carriers as text messages). All duplicates were removed; each `task-N.md` now ends with exactly one appendix using the DB-verified counts above.

## Fixture re-capture (deterministic, for the committed SC-007 test)

The original capture (this file's "Prompt fixture" section above) ran against the real `~/.joey` home, so it embedded machine-specific memory/profile/skills content and could not back a committed cross-machine test. It is kept verbatim as `pre-change-prompt-realhome.txt` (historical record). The committed fixture `pre-change-prompt.txt` was re-captured with pinned deterministic inputs: `JOEY_HOME=/tmp/f031-empty-home` (empty dir — no SOUL.md, memories, skills), `TERMINAL_CWD` and `JOEY_ENVIRONMENT_HINT` unset, same probe binary (`/tmp/prompt-probe`, built 2026-09-15 20:25 from the clean tree at `420bcab`, i.e. pre-change code), `Config::defaults()`, default toolsets, model `parity-model`, provider `parity-provider`, `pass_session_id: false`. The parity test (`system_prompt_token_neutrality_sc007`) pins its post-change side to the same empty-home condition via `HomeOverrideGuard` and normalizes the wall-clock "Conversation started:" line on both sides.
